# Docker 환경 설정 및 빌드 관련 파일
# 도커 환경에서 생성되는 불필요한 파일 목록
.DS_Store
*.log
npm-debug.log*

# Node.js에서 사용하는 의존성 모듈 폴더 (매우 큼)
/node_modules

# ⚠️ MariaDB DB 데이터 폴더 (필수 제외)
# 이 폴더에는 DB의 실제 데이터 파일이 들어있으므로, Git에 커밋하면 안 됩니다.
/Database